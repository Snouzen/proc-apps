const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const users = await prisma.user.findMany({ select: { email: true, role: true, regional: true, siteArea: true } }); console.log(users.filter(u => (u.email || '').toLowerCase().includes('spb'))); } main();
